# Stage 5 — WSL2 (Ubuntu 26.04), CPython 3.13, GPU. Identical to Stage 3's environment:
# this stage changes the conditioner, not the toolchain, and reusing the environment
# unchanged is part of keeping "one variable per stage" honest. Nothing new was installed.
#
# The Python version is STILL load-bearing (pycbc has no cp314 wheel) — see Stage 1.

lalsuite
pycbc

gwpy
gwosc
torch

numpy
scipy
matplotlib
h5py
pandas         # Gravity Spy metadata (Zenodo 5649212), via stage3_dataset.make_specs

scikit-learn
tqdm
