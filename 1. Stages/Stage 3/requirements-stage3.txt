# Stage 3 — WSL2 (Ubuntu 26.04), CPython 3.13, GPU. Stage 2's environment plus pandas
# (the Gravity Spy classification table is a 90 MB CSV; installed 2026-07-12 via
# `uv pip install pandas`).
#
# The Python version is STILL load-bearing (pycbc has no cp314 wheel) — see Stage 1.

lalsuite
pycbc

gwpy
gwosc
torch

numpy
scipy
matplotlib
h5py
pandas         # Gravity Spy metadata (Zenodo 5649212)

scikit-learn
tqdm
